name: My first workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repo
          uses: actions/checkout@v5

        - name: List and read files
          run: | 
            echo "My first Github Actions Job"
            ls -ltra
            cat README.md

        - name: Generate ASCII artwork
          run: cowsay -f dragon "Reun for cover, I am a DRAGON....RAWR" >> dragon.txt

        #- name: List files
        #  run: ls

        #- name: Read file
        #  run: cat README.md